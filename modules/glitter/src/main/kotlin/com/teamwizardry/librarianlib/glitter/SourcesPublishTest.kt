package com.teamwizardry.librarianlib.glitter

/**
 * For some reason, the previous snapshot's sources appear at the same time as the current snapshot is uploaded.
 * If this file is present in the previous snapshot's sources, then we know that they're *actually* the
 * *current* snapshot's sources that have been mislabeled.
 */
class SourcesPublishTest {
}