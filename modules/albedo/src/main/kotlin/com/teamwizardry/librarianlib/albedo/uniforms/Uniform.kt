package com.teamwizardry.librarianlib.albedo.uniforms

/*
import com.teamwizardry.librarianlib.albedo.Shader
import com.teamwizardry.librarianlib.albedo.logger

abstract class Uniform<T>(val program: Shader, val name: String, val type: UniformType, val size: Int, val location: Int) {
    abstract fun set(value: T)

    class NoUniform(owner: Shader, name: String, type: UniformType,
        size: Int, location: Int) : Uniform<Any?>(owner, name, type, size, location) {
        init {
            logger.warn("[Shader %s] Uniform `%s` has unsupported type `%s`", owner.glName, name, type.name)
        }

        override fun set(value: Any?) {
            // nop
        }
    }
}
 */
